---
interface Props {
	tags: string[];
}
import Tag from "../components/Tag.astro";
const { tags } = Astro.props;
---
<div class="flex flex-row gap-x-2 pl-4">
<span class="text-teal-300">Tags:</span>{tags.map((t) => <Tag tag={t} />  )
}
</div>